{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\" ?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{ range .Site.Pages }}
    {{ $permalink := .Permalink }}

    {{ if not (or
      (strings.HasSuffix $permalink "/search/")
      (strings.Contains $permalink "/tags/")
      (strings.Contains $permalink "/categories/")
    ) }}

      <url>
        <loc>{{ .Permalink }}</loc>
        {{ with .Lastmod }}
          <lastmod>{{ .Format "2006-01-02T15:04:05-07:00" }}</lastmod>
        {{ end }}
      </url>

    {{ end }}
  {{ end }}
</urlset>
